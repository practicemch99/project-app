server:
  address: 127.0.0.1
spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3307/appdb?sslMode=DISABLED&allowPublicKeyRetrieval=true&connectionTimeZone=Asia/Seoul&forceConnectionTimeZoneToSession=true}
    username: ${DB_USERNAME:app_core}
    password: ${DB_PASSWORD:local-core-only}
